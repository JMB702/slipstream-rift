{
  "name": "@slipstream-npc/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo 'shared is consumed via tsconfig paths; nothing to build'",
    "dev": "echo 'shared has no dev step'"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
