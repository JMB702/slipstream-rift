node_modules/
dist/
.turbo/
.partykit/
.vercel/
.claude/
.DS_Store
*.log
*.tsbuildinfo
.env
.env.local
.env.*.local
coverage/

# Mixamo source FBX files — kept locally for re-merging via /tmp/merge_mixamo.py.
# Only the merged Soldier.glb (in apps/client/public/models/) ships in the repo.
Animations/
Characters/
*.fbx

# 3D source assets (RAR/FBX/blend) — kept locally, never committed.
3D Assets/
/Maps/
logs/
